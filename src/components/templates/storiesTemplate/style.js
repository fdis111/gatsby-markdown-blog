import styled from "styled-components";

const StoriesTemplateContsainer = styled.div`
  display: flex;
  flex-direction: column;

  & > div {
    padding: 1.5rem 0;
  }
`;

export default StoriesTemplateContsainer;
